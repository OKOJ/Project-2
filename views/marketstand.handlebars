<div class="row">

    {{!-- <div class="author-container">
					<h1>Vendors Name {{user.name}}</h1>
					<table class="table table-striped">
						<thead>
							<tr>
								<th>Name</th>
								<th># of Posts</th>
								<th>See Author's Posts</th>
								<th>New Post</th>
								<th>Delete Author</th>
							</tr>
						</thead>
						<tbody>
							<tr id="form-row">
								<form id="author-form">
									<td colspan="4">
										<input placeholder="Enter a name" id="author-name" type="text" />
									</td>
									<td><button type="submit" class="btn btn-success">Create Author</button>
									</td>
								</form>
							</tr>
						</tbody>
					</table>
				</div> --}}
    {{!-- <img src="\img\tomatoes-1459069_960_720.jpg" alt=""> --}}
    {{!-- <div>
        <ul id="example-list" class="list-group">
            {{#each user.name}}
            <li data-id="{{this.id}}" class="list-group-item mb-2">
                <a href="example/{{this.id}}">{{this.text}}</a>
                <button class="btn btn-outline-success float-right delete">ｘ</button>
            </li>
            {{/each}}
        </ul>
    </div> --}}
     <div class="card-body">
        <h5 class="card-title">name: {{user.name}}</h5>
       <p>
          <strong>ID</strong>: {{user.example}}</p>
        <p> 
        <p>
            <strong>Name</strong>: {{example.text}}</p>
        <p>
            <strong>Price</strong>: {{example.price}}</p>
        <p>
            <strong>Quantity</strong>: {{example.quantity}}</p>
        <p>
            <strong>Image</strong>: <img src="{{example.image}}" height="200" alt="Image preview..."></p>
        <p>
            <strong>Description:</strong> {{example.description}}</p>

    </div>

{{!--
    {{#if user}}
    <a href="/profile">← Back To Home</a>
    {{/if}}
    {{#unless user}}
    <a href="/market">← Back To Home</a>
    {{/unless}} --}}
</div>